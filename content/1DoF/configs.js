export let palette = { 
    backgroundFill: "#202020",

    pointStroke: "#ff9200", 
    pointFill: "#000000",

    labelFill: "#1fced9",

    segmentFill: "#20c5c5",
};

export let styles = {
    pointRadius: 15,
    pointStrokeWeight: 3,

    labelTextSize: 35,
    labelOffsetX: 30,
    labelOffsetY: 0,
    
    segmentArrowSize: 15,
    segmentWeight: 3
}

export let canvasConfig = {
    trimX: 0,
    trimY: 0,
}

export let axisControls = {
    x: -350,
    y: 0,
    w: 500,
    h: 500,
    size: 500,
}

export let sliderOffset = { 
    x: 198, 
    y: -11 
}
