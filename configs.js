let palette = { 
    backgroundFill: "#202020",

    pointStroke: "#ff9200", 
    pointFill: "#000000",

    labelFill: "#1fced9" 
};

let styles = {
    pointRadius: 15,
    pointStrokeWeight: 3,

    labelTextSize: 35,
    labelOffsetX: 30,
    labelOffsetY: 0,
}

let canvasConfig = {
    trimX: 450,
    trimY: 200
}
