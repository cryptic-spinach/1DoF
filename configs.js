let palette = { 
    backgroundFill: "#202020",

    pointStroke: "#ff9200", 
    pointFill: "#000000",

    labelFill: "#1fced9",

    segmentFill: "#20c5c5"
};

let styles = {
    pointRadius: 15,
    pointStrokeWeight: 3,

    labelTextSize: 35,
    labelOffsetX: 30,
    labelOffsetY: 0,
    
    segmentArrowSize: 15
}

let canvasConfig = {
    trimX: 550,
    trimY: 150
}

let axisOffset = {
    x: -300,
    y: 0,
}
